    name: Setup SSH
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    name: Deploy to Hetzner
      run: |
        ssh youssef@94.130.168.61 << EOF
          cd /
          git pull
          # Any other deployment commands
        EOF
